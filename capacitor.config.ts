import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tonetxo.criosfera',
  appName: 'Criosfera Armónica',
  webDir: 'dist'
};

export default config;
